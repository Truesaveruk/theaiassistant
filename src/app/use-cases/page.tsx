import PagePlaceholder from "@/components/PagePlaceholder";

export const metadata = { title: "Use cases" };

export default function Page() {
  return (
    <PagePlaceholder
      title="Use cases"
      intro="Real jobs, done with AI, start to finish. What the problem was, which tool solved it, and how long it took compared with doing it by hand."
      detail="Being written now, from actual work rather than invented examples."
    />
  );
}
