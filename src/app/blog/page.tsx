import PagePlaceholder from "@/components/PagePlaceholder";

export const metadata = { title: "AI news, decoded" };

export default function Page() {
  return (
    <PagePlaceholder
      title="AI news, decoded"
      intro="What actually happened in AI this week and what it means if you run a small business. Two minutes a post."
      detail="The news feed starts once the site is connected to its database. Every post is written from a published source and cites it at the foot of the article."
    />
  );
}
